# План разработки — «График смен» (shifts-app)

## 1. Инфраструктура и окружение

- [x] Проверить версии Node.js, pnpm, установленные в проекте.
- [x] Убедиться, что все зависимости установлены (pnpm install).
- [x] Настроить переменные окружения (`.env.local`):
  - `NEXT_PUBLIC_SUPABASE_URL`
  - `NEXT_PUBLIC_SUPABASE_ANON_KEY`
  - (опционально) `ALLOWED_DEV_ORIGINS` для разработки.
- [x] Проверить работу dev-сервера (`pnpm dev`), убедиться, что страница открывается.
- [x] Настроить ESLint и Prettier (если не настроены) – проверить конфиги.
- [x] Настроить TypeScript (уже есть), проверить `tsconfig.json` и отсутствие ошибок типов.
- [x] Настроить пути импортов (alias `@/` уже есть).

---

## 2. Модели данных и типы (пакет `@shifts/types`)

- [x] **Проверить и дополнить** Zod-схемы в `packages/types/src/index.ts`:
  - [x] `ShiftType` – добавить поля `createdAt`/`updatedAt` (опционально).
  - [x] `Shift` – добавить поле `userId` (если поддержка нескольких пользователей).
  - [x] `Preset` – убедиться, что `sequence` — массив `ShiftType.id`.
  - [x] `PresetMeta` – добавить поле `userId` и индексы для оптимизации.
  - [x] Создать схемы для валидации форм (например, для создания/редактирования типов смен, пресетов).
- [x] Экспортировать все схемы и типы из основного файла.
- [x] Написать unit-тесты для схем (опционально, но желательно).

---

## 3. Работа с данными (Supabase + React Query)

- [x] **Создать клиент Supabase** (`shared/lib/supabase.ts`) с настройками из env.
- [x] **Определить типы таблиц** в Supabase (можно использовать автоматическую генерацию типов через `supabase gen types`).
- [x] **Создать API-сервисы** для взаимодействия с таблицами:
  - [x] `shiftTypesApi` – CRUD для типов смен.
  - [x] `shiftsApi` – CRUD для смен (по дате, по диапазону, по пользователю).
  - [x] `presetsApi` – CRUD для пресетов.
  - [x] `presetMetaApi` – работа с метаданными пресетов.
- [x] **Настроить React Query**:
  - [x] Создать `QueryClient` с настройками (по умолчанию, staleTime, retry).
  - [x] Обернуть приложение в `QueryClientProvider`.
  - [x] Написать хуки для каждого API (useQuery, useMutation) с инвалидацией кэша.
- [ ] **Добавить поддержку офлайн/локального хранилища** (persistQueryClient) для работы без интернета (если нужно).

---

## 4. Управление состоянием (Zustand)

- [ ] **Создать хранилище для текущего состояния приложения**:
  - [ ] `useAppStore` – хранит выбранную дату, отображаемый месяц/год, текущий пользователь (если есть).
  - [ ] `useShiftStore` – кэширует загруженные смены для быстрого доступа (можно совместить с React Query).
  - [ ] `usePresetStore` – хранит список пресетов и текущий выбранный пресет.
- [ ] Интегрировать Zustand с React Query (синхронизация при мутациях).

---

## 5. UI-компоненты (уже есть базовые)

- [x] **Проверить и адаптировать** существующие компоненты:
  - [x] `Button`, `Badge`, `Card`, `Dialog`, `Sheet`, `DropdownMenu`, `Select`, `Input`, `Textarea`, `Label`, `Separator`, `Calendar`, `Toaster`, `Toast`.
  - [x] Убедиться, что все компоненты используют `cn()` и корректно рендерятся.
  - [ ] Добавить недостающие компоненты (например, `Switch`, `Checkbox`, `RadioGroup`, `Tabs`, `Tooltip`, `Popover`).
- [x] **Создать композитные компоненты** для бизнес-логики:
  - [x] `ShiftTypePicker` – выбор типа смены с цветом и эмодзи (реализован через Select).
  - [x] `PresetSelector` – выбор пресета из списка.
  - [x] `DateNavigator` – навигация по месяцам/неделям.
  - [x] `ShiftCell` – отображение одной смены в календаре.
  - [x] `ShiftForm` – форма для создания/редактирования смены.
  - [x] `PresetForm` – форма для создания/редактирования пресета.
- [x] **Настроить тему и кастомизацию** (через `globals.css` и `next-themes` уже есть).

---

## 6. Страницы и маршруты

- [x] **Главная страница (`/`)** – календарь на месяц с отображением смен.
  - [x] Отображать сетку календаря (7 колонок).
  - [x] Каждая ячейка – день, клик открывает диалог редактирования смены.
  - [x] Возможность переключать месяц/год.
  - [ ] Индикация выходных/праздников (опционально).
- [x] **Страница управления типами смен (`/settings/shift-types`)**:
  - [x] Таблица/список существующих типов.
  - [x] Кнопка "Добавить" – открывает диалог с полями: название, цвет, эмодзи, длительность, категория.
  - [x] Редактирование и удаление.
- [x] **Страница управления пресетами (`/settings/presets`)**:
  - [x] Список пресетов с возможностью редактирования.
  - [x] Форма создания пресета: название и последовательность типов смен (Drag-and-drop или мультиселект).
  - [x] Возможность применить пресет к выбранному диапазону дат (с выбором режима: overwrite, fill-empty, continue).
- [ ] **Страница настроек приложения** (если нужна) – тема, локаль, и т.п.

---

## 7. Бизнес-логика (применение пресетов)

- [x] **Реализовать функцию применения пресета** на выбранный интервал дат:
  - [x] Получить `preset.sequence` (массив id типов смен).
  - [x] Учесть `PresetMeta.referenceDate` и `referenceIndex` для продолжения с правильной позиции.
  - [x] Поддержать три режима:
    - `overwrite` – заменять все существующие смены.
    - `fill-empty` – заполнять только пустые дни.
    - `continue` – продолжить последовательность, начиная с последней применённой позиции.
  - [x] Сохранить обновлённый `PresetMeta` после применения.
- [ ] **Обработка конфликтов** – что делать, если на дату уже есть смена (спросить пользователя).
- [ ] **Добавить прогресс-индикатор** при массовом применении.

---

## 8. Интеграция и тестирование

- [ ] Написать интеграционные тесты для API-сервисов (используя mock или реальный Supabase).
- [ ] Написать тесты для бизнес-логики (применение пресетов) с использованием Jest/Vitest.
- [ ] Написать сквозные (E2E) тесты для основных пользовательских сценариев (Playwright/Cypress).
- [x] Настроить CI/CD (GitHub Actions) для автоматической проверки типов, линта и тестов (CI workflow уже есть).

---

## 9. Деплой и публикация

- [x] Настроить сборку для GitHub Pages (уже есть скрипт `build:gh-pages`).
- [x] Проверить production-сборку локально (`pnpm build` и `pnpm start`).
- [x] Настроить переменные окружения для продакшена (секреты в GitHub).
- [x] Выполнить деплой на GitHub Pages.
- [x] Добавить документацию по развертыванию и настройке (в README).

---

## 10. Документация и поддержка

- [x] Обновить `README.md` с описанием проекта, технологий, инструкциями по установке и запуску.
- [ ] Добавить `CONTRIBUTING.md` (если проект открытый).
- [ ] Создать пользовательскую документацию (руководство по использованию).
- [ ] Настроить систему ошибок и логирование (сбор ошибок в production).

---

## 11. Дополнительные улучшения (бэклог)

- [ ] Поддержка нескольких пользователей (аутентификация через Supabase Auth).
- [ ] Экспорт/импорт графика (CSV, iCal).
- [ ] Уведомления о предстоящих сменах (через Service Worker или email).
- [ ] Мобильная адаптация (уже есть отзывчивые компоненты, но проверить).
- [ ] Аналитика (статистика по типам смен, переработка и т.п.).
- [ ] Международная локализация (i18n).

---

### 📌 Новые задачи (добавлены по ходу разработки)

- [x] Настроить Husky и lint-staged для pre-commit (проверка линта, форматирования, типов).
- [x] Исправить отображение выбранного типа смены в Select (вместо UUID показывать название с эмодзи).
- [x] Заменить `watch` на `useWatch` для устранения предупреждений линтера.
- [ ] Написать документацию для разработчиков по настройке проекта локально.
- [ ] Добавить `db/` папку с SQL-скриптами в репозиторий.

---

**Примечание:** Порядок выполнения пунктов может меняться. Завтра планируем заняться Zustand, интеграционными тестами и обработкой конфликтов при применении пресетов.
